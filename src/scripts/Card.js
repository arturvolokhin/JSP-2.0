export function CreateNewCard(titleText, cardTextInput, cardAuthor, cardDate, cardId) {
    this.title = titleText; 
    this.comment = cardTextInput; 
    this.date = cardDate;
    this.author = cardAuthor;
    this.id = cardId;
}
